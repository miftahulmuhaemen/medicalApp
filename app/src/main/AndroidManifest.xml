<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ega.medicalapp">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name="androidx.multidex.MultiDexApplication"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.psychologist.ChatPsychologistActivity"/>
        <activity android:name=".ui.psychologist.PsychologistActivity" />
        <activity android:name=".ui.user.chat.ChatUserActivity" />
        <activity android:name="com.ega.medicalapp.ui.user.health.article.detail.ArticleDetailActivity" />
        <activity android:name=".ui.user.journal.JournalActivity" />
        <activity android:name=".ui.user.profile.ProfileActivity" />
        <activity android:name=".ui.user.UserActivity" />
        <activity android:name=".ui.register.RegisterActivity" ></activity>
        <activity android:name=".ui.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>